# Processed data (staging)

Reserved for cleaned or aggregated outputs used by Power BI or repeat analysis (for example session-level funnel flags, monthly KPI snapshots).

Raw source files remain in `data/raw/`. Nothing in this folder is required to run the SQL or Python validation workflow today.
